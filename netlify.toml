# Netlify deploy config for Atelier.
#
# The app is pre-built and committed to dist/ (regenerated by `node build/build.js`;
# see DEPLOY.md), so Netlify just serves that folder — there is no build step.
# Pinning this here means deploys no longer depend on the dashboard settings.
[build]
  publish = "dist"
  command = ""
